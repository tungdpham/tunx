How do I make a graph partitioner that's compute and bandwidth aware? It only assumes that the machines have sufficient memory. The parallelizations strategy for training is modelled as a pipeline. Say, the pipeline consists of node A, B, and C. Say the transmission cost from A-> B, B->A, B->C, C->B,... are not the same, and the compute power of A,B,C are also heterogeneous. We can model the bottleneck speed as the maximum(compute time on each node, tranmission time between 2 contiguous nodes). How we partition each node will affect communication  time. It is modeled as the volume of boundary intermediate activation tensors / link speed of each the 2 node. The compute subgraph partition also affect compute time, say each node takes C units of work then speed is C / compute speed of the node. How do I minimize the bottleneck speed induced by partitioning the subgraph assuming that we will give each node a connected subset of operators.   