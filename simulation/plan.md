Instead of doing 2 phase topo and reverse topo which can lose information. Can you do this instead: we will just do 1 topo order macro merging BUT we will merge with different rules. If it is purely linear: X has sole dependent that is Y and Y only has X as parent, then it is safe to merge this. If it is a joining point, which means X has sole dependent that is Y, has no peers, and Y depends on multiple macros, then, for each pair of parent X1, X2,..., Xn, we find their common ancestor. The ones with ancestor at closest ancestor is branch merged first, then comes ones with 